{
  "name": "mondrians",
  "version": "1.0.0",
  "description": "Magic Mondrian NFT launch monorepo",
  "main": "index.js",
  "repository": "https://github.com/whitelabel-market/mondrians.git",
  "author": "Whitelabel Solutions",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "contracts"
  ],
  "scripts": {
    "backend:dev": "yarn workspace backend dev",
    "dev": "yarn frontend:dev && yarn backend:dev",
    "frontend:dev": "yarn workspace frontend serve",
    "frontend:build": "yarn workspace frontend build",
    "frontend:lint": "yarn workspace frontend lint"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.0",
    "@commitlint/config-conventional": "^17.0.0",
    "commitizen": "^4.2.4",
    "husky": "^8.0.1"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
